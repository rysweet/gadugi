"""
Shared Enhanced-Separation code package.

This file marks `.gadugi.shared` as a regular Python package so that
import statements such as

    import src.src.shared.state_management

resolve correctly during static analysis (Pyright) and at runtime.
"""
