# Gadugi Environment Variables
# Copy this file to .env and update with your actual values

# Neo4j Configuration
# Authentication (username/password format)
NEO4J_AUTH=neo4j/your_secure_password_here

# Connection settings - using non-default ports to avoid conflicts
NEO4J_HOST=localhost
NEO4J_BOLT_PORT=7689  # Non-default Bolt port (default would be 7687)
NEO4J_HTTP_PORT=7475  # Non-default HTTP port (default would be 7474)
NEO4J_DATABASE=gadugi

# Memory and performance tuning for Neo4j
NEO4J_PAGECACHE_SIZE=1G        # Page cache size for better read performance
NEO4J_HEAP_INITIAL_SIZE=1G     # Initial heap size
NEO4J_HEAP_MAX_SIZE=2G         # Maximum heap size

# Docker Compose Environment
COMPOSE_PROJECT_NAME=gadugi

# Production Security Notes:
# - Change NEO4J_AUTH to use a strong, unique password
# - Never commit the actual .env file to version control
# - Consider using Docker secrets or a secrets management service for production
# - The non-default ports (7475, 7689) help avoid conflicts with other Neo4j instances
